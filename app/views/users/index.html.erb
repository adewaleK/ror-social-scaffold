
<div class="other-users">
<div class="others">
<% @users.each do |user|%>
  <div class="users-details">
    <h3><%= user.name %></h3>
    <h3><%= link_to 'See Profile',  user_path(user), class: 'profile-user' %></h3>
    <%= link_to 'Invite to Friendship',  user_path(user), class: 'request-btn' %>
  </div>
<% end %>
</div>
<h3>Friends</h3>
</div>
